export default function filterEventsReducer (state = [], action) { /*
    switch (action.type) {
        case 'FILTER_LOCATION':
            const newState = 
            console.log(newState);
            return newState;

        default:
            return state
      } */
}